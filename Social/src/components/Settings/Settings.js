import React from "react";
import settingsStule from "./Settings.module.css";

const Settings = (props) => {
    return (
        <div className={settingsStule.settings}>
            Settings
        </div>
    )
}

export default Settings;